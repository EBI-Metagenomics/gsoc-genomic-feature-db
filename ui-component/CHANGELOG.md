# Changelog

All notable package changes will be recorded here before an approved release.

## 0.1.0 - local implementation

- Add the private tarball-only `genomic-feature-db-component` package.
- Export the composed `GenomicFeatureBrowser` and domain types.
- Preserve packaged SQLite worker/WASM loading and embedded JBrowse navigation
  with replacement interval highlighting.
